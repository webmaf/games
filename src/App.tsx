import "./App.css";
import Mastermind from "./components/games/Mastermind.tsx";

function App() {
  /**
   * keyboardCommandLearnings
   * ============================
   * tastatur jump markierungen
   * cut paste
   * cmd + d = multicursor
   * cmd + shift + p = auswahlpalette
   */

  return (
    <>
      <Mastermind/>
    </>
  );
}

export default App;
